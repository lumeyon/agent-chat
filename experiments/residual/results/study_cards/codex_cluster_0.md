# Study Card — codex cluster 0

## Failure Mode
**n_latex dominant (+0.9σ)** (n=33 / 99 high-residual rows = 33%)

## Residual Signature
Driving features (mean signed residual, in z-score units across high-residual rows):

- `n_latex`: +0.87σ (mean abs 0.87)
- `n_self_correction`: -0.47σ (mean abs 0.68)
- `n_codeblocks`: +0.57σ (mean abs 0.65)
- `n_hedge_words`: -0.40σ (mean abs 0.50)

## Exemplars (top 5 most-extreme cluster members)

### 1. `recWzUUYBN0NuXO2k` [Chemistry/Chemistry (general)] (residual ‖² = 24.29)

> Initial moles acetic acid:

```text
0.02000 L * 0.050 M = 0.00100 mol
```

After dilution, total acid moles are unchanged. Equivalence requires:

```text
0.00100 mol / 0.100 M = 0.01000 L = 10.00 mL N

### 2. `recPzW1WqRnPs57D6` [Physics/Quantum Mechanics] (residual ‖² = 16.73)

> After the new field is along `+Y`, the Hamiltonian is

```text
H = -γ B S_y
```

The initial `+Z` spin state is a superposition of the two `S_y` eigenstates, whose energy separation is

```text
ΔE = γ

### 3. `rec1oj2DveQWl9Rpw` [Chemistry/Chemistry (general)] (residual ‖² = 14.76)

> Moles of `Fe(OH)3`:

```text
0.100 g / 106.9 g mol^-1 = 9.36e-4 mol
```

Stoichiometric acid needed:

```text
Fe(OH)3 + 3H+ -> Fe3+ + 3H2O
3(9.36e-4) = 2.81e-3 mol H+
```

In `0.100 L`, `[Fe3+] ≈ 9.36

### 4. `recG1ljJQcVy6CbSl` [Physics/Physics (general)] (residual ‖² = 14.54)

> For \(A_y=\frac{h}{4\pi}\begin{pmatrix}0&-i\\ i&0\end{pmatrix}\), the matrix is proportional to \(\sigma_y\), whose eigenvalues are \(\pm 1\). So the eigenvalues of \(A_y\) are real: \(\pm \frac{h}{4\

### 5. `recRTiyaRymerDJ4y` [Physics/Physics (general)] (residual ‖² = 8.65)

> For equal albedo around the same star, equilibrium temperature scales as:

\(T \propto a^{-1/2}\)

Using Kepler’s third law:

\(a \propto P^{2/3}\)

So:

\(T \propto P^{-1/3}\)

Planet_4 has period ra

## What to learn

Cluster pattern: n_latex dominant (+0.9σ). No predefined lesson — examine the exemplar responses above and characterize manually.